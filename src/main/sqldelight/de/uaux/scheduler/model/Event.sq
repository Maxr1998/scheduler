CREATE TABLE Event (
    id INTEGER PRIMARY KEY,
    name TEXT NOT NULL,
    module TEXT NOT NULL,
    participants INTEGER AS Int DEFAULT NULL CHECK (participants > 0)
);

queryAll:
SELECT e.*, s.semester, s.required FROM Event e JOIN StudycourseEvents s ON e.id = s.event WHERE s.studycourse = ? ORDER BY name;